# deeplearn

A deep learning library built from scratch, module by module.

## Setup

```bash
pip install -e ".[dev]"
```

## Run tests

```bash
pytest tests/
```

## Structure

```
deeplearn/        ← package
tests/            ← one test file per sub-package
```
